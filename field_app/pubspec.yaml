name: integin_field_app
description: Offline-first INTEGIN inspection field application.
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  crypto: ^3.0.3
  shared_preferences: ^2.5.3
  flutter_secure_storage: ^11.0.0
  http: ^1.6.0
  cryptography: ^2.9.0
  image: ^4.9.2
  sqflite_sqlcipher: ^3.4.1
  path: ^1.9.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  sqflite_common_ffi: ^2.4.3

flutter:
  uses-material-design: true
