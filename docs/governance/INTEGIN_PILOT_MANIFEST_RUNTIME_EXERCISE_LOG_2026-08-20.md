# INTEGIN Pilot Manifest Runtime Exercise Log — 2026-08-20

**Status:** Authorized; workspace recordkeeping restored and operational trust root restricted; owner-authorized private variable-names-only audit pending.

## Authority and objective

The owner separately authorized continuation of the controlled isolated-pilot manifest runtime exercise on 2026-08-20. This authorization applies only to the approved, isolated manifest candidate and fixture boundary. Its objective is to prove the source-derived receipt path with non-secret evidence and cleanup controls; it is not authorization to modify acceptance, activate package enforcement, use external systems, start OIDC, initialize or unseal OpenBao, inspect private material, or restore pilot data.

## Ordered controls

| Step | Required evidence | Stop condition |
| --- | --- | --- |
| Preflight | Acceptance `/healthz` and `/readyz` are both `200`; no pre-existing listener on `127.0.0.1:18080`; package enforcement and OIDC remain disabled; OpenBao remains sealed/unwired; approved wrapper/launcher/fixture boundary is present. | Any failed or ambiguous boundary check. |
| Isolated exercise | Candidate binds only to `127.0.0.1:18080`; fixture is invoked only through its approved wrapper; no protected payload is emitted. | Public/non-loopback bind, unexpected enforcement state, protected-material exposure, or acceptance degradation. |
| Evidence | Only approved redacted receipt adapter and metadata-only checks are used. | Any receipt shows an unexpected acceptance or invalid-path success. |
| Cleanup | Candidate is stopped; no listener remains; acceptance returns `200/200`. | Candidate fails to terminate or acceptance fails its post-check. |
| Independent review | Runtime claims are reconciled against non-secret command and adapter evidence. | Material evidence gap or unresolved high-severity objection. |

## Evidence discipline

The final record will state what was and was not proven. It will exclude private configuration, fixture body, proof/manifest payloads, database credentials, keys, tokens, customer data, and any protected runtime material. Any pilot database restoration remains a separate destructive-data decision and is not authorized by this exercise.

## Preflight result

The non-mutating preflight passed on 2026-08-20. The receipt-propagation structural validation returned `RECEIPT_PROPAGATION_STATIC_CHECKS_PASSED`; protected acceptance returned HTTP `200` from both `/healthz` and `/readyz`; no listening process existed on `127.0.0.1:18080`; the required listener-ownership capability was available; the recovery asset was confirmed present by metadata-only existence check; and the required public wrapper, launcher, candidate, public fixture handle, v2 adapter, and v2 schema were present. No private configuration was opened, no candidate was launched, and no pilot data was restored during this phase.

## Isolated-exercise attempt 1 — stopped fail-closed

The approved public wrapper was invoked on 2026-08-20 after preflight. It stopped before candidate launch or fixture invocation because its trusted-path guard rejected the `C:\MY PROJECT` root as having a reparse point or an unsafe broad-write ACL. No candidate listener was created, no private configuration was read by the launcher, no receipt context was created, no fixture was built or invoked, and no pilot data was changed. The next action is a read-only ACL/reparse diagnostic of the trusted root and required runtime ancestry; the wrapper will not be retried unchanged.

The first independent-review selector invocation was rejected before any review or repository operation because `runtime` is not a supported decision dimension. The retry will use the supported `security`, `operations`, and `testing` dimensions with the required live-runtime flag. This selector-input error does not affect the runtime boundary or any INTEGIN state.

The first redacted LLM review runner did not produce a usable result because the initial model response had no parseable JSON content. The runner stopped before producing a recommendation, no reviewer result was counted, and no source, ACL, candidate, fixture, or private material was changed or accessed. The follow-up will use a single model and return its raw constrained JSON response with explicit empty-response handling rather than retrying the failed parsing path.

The revised single-model invocation also returned non-parseable response content before a review could be accepted. It did not produce a recommendation and was not counted as evidence. A final, materially different review attempt will remove proxy schema enforcement and extended-thinking controls, require plain JSON in the prompt, and retain the raw response for explicit validation. If that attempt is not usable, the runtime-boundary repair will remain blocked and the limitation will be reported rather than retried again.

The final bounded independent review returned `ship` with no high-severity blocker. It accepted excluding `PropagationFlags.InheritOnly` only when evaluating whether an ACE applies to the current object, while retaining the reparse-point, unresolved-identity, broad-SID, applicable-write, and all-component checks. It required actual guard coverage for both inheritance-only and child-applicable broad-write cases.

The owner implemented that narrow source-only repair in both the public wrapper and launcher. `test-receipt-propagation-static.ps1` passed, and the new `test-acl-guard-inheritonly.ps1` passed against temporary local directories. The behavioral test proves the actual extracted guard functions treat an `Authenticated Users` `Modify` ACE marked `InheritOnly` as non-applicable to its template directory, while still rejecting the child directory where that ACE becomes applicable. These tests do not start a candidate, call HTTP, read private configuration, inspect a fixture, or alter an ACL.

The administrator restored the root ACL from its preserved rollback backup after the original connected session could not obtain the required restore privilege. Acceptance remained `200/200` and the candidate listener remained absent. Because the general workspace root is intentionally broadly writable again for stable workspace recordkeeping, the public wrapper and launcher now use the independently restricted non-private `operations\pilot\runtime` directory as their operational trust root. Structural propagation and ACL-guard tests passed after this focused repair. Private configuration remains uninspected pending the owner's separate variable-names-only authorization.

Under the owner's later variable-names-only authorization, the private candidate environment was audited without emitting any private name or value. The aggregate result was 21 declared names, 11 names outside the launcher allow-list, one duplicate allowed-name group, zero invalid-syntax rows, and a passed required-name check. Ten unapproved names are not known launcher-generated names. The audit authorization does not permit choosing between duplicate values, deleting unknown configuration, or expanding the child-environment allow-list; the runtime remains blocked until the owner corrects the file or grants a separately bounded comparison/correction authority.

Under the owner's subsequent non-disclosing correction authorization, the unsupported and launcher-generated private entries were removed and the one equality-confirmed duplicate allowed entry was collapsed. A private rollback backup was created before replacement. Aggregate-only validation reports nine declared names, zero unapproved names, zero duplicate names, zero invalid-syntax rows, and a passed required-name check. No private name or value was emitted, logged, uploaded, or committed.

## Controlled runtime execution and evidence result

After the environment correction, the approved wrapper successfully started the isolated loopback candidate and invoked the approved fixture with exit code `0`. The wrapper’s public status showed protected acceptance at `200/200` both before and after the run. The candidate was then stopped, `127.0.0.1:18080` had no listener, the fixture signal root had no remaining artifacts, and the latest restricted `candidate-control` directory had no remaining artifacts.

The public v2 adapter was executed against an evidence-capturing opaque receipt run and its public wrapper record. Its matrix result was **`incomplete`**: all eight expected receipt cases were `not_run`. Therefore, this run proves isolated candidate launch, approved fixture execution, boundary cleanup, and protected acceptance preservation only. It does **not** prove the valid-proof, Field verified-cache, replay-rejection, signature-invalid, package-hash-invalid, expired, authority-mismatch, or key-unknown receipt cases. No private content, receipt payload body, or opaque identifier was disclosed in this record.

The isolated-exercise retry still stopped fail-closed before candidate launch, fixture invocation, receipt-context creation, or private launcher configuration access. The same trusted-root error indicates at least one additional applicable broad-write or unresolved ACE exists in the runtime ancestry; the `InheritOnly` repair is therefore insufficient for this host. No retry will occur until a new read-only ACL diagnosis identifies the exact remaining rule and a separate bounded repair is reviewed.

The follow-up read-only diagnostic identified one applicable `NT AUTHORITY\Authenticated Users` (SID `S-1-5-11`) allow ACE with write-like rights on `C:\MY PROJECT`; it was not marked `InheritOnly`. This is a real trust-root violation rather than a wrapper false positive. Before any access-control change, acceptance was reconfirmed at `200/200`, the candidate listener was reconfirmed absent, and a root-only ACL rollback backup was saved under `MANUS_AGENT_WORKSPACE\acl-backups`. The planned repair is limited to the non-private operational ancestry, preserves the current user, SYSTEM, and Administrators access, and has no recursion into private material.

## Isolated fixture provisioning and matrix visibility stop — 2026-08-20

The fixture-provisioning script was corrected to invoke the local pilot PostgreSQL administrator through the confirmed stdin-safe `exec psql ... -At` form. It completed with its public `passed` status, protected acceptance remained `200/200`, and no candidate listener was present. To validate the inconsistency in the prior status record, the source-owned fixture was executed with an isolated signal directory and its generated public SQL transaction was applied to `integin-pilot-postgres` through that same configured administrator. Public metadata confirmed a valid transaction shape, fixture exit `0`, SQL apply exit `0`, and one matching pilot package row under the container administrator. No private value, credential, fixture body, proof, key, or identifier was emitted.

The application-role matrix diagnostic still returned zero aggregate identity rows. A non-disclosing comparison confirmed that the application database target and container-configured database are the same. Source inspection established that this is a row-level-security session-context gap: production work-package access requires a transaction-local `integin.tenant_id` and `integin.organization_id` scope, whereas the matrix driver opened bare database queries. This is not evidence of a missing fixture and must not be addressed by reseeding, policy changes, disabling RLS, or acceptance modification. The controlled matrix is paused before candidate launch while a bounded driver repair uses the established scoped transaction contract for identity, mutation, restoration, and diagnostic operations. The package-enforcement, OIDC, OpenBao, acceptance, and fixture secrecy boundaries remain unchanged.

## Final authorized eight-case matrix result — passed — 2026-08-20

The owner-authorized matrix was completed through the approved wrapper and read-only public v2 adapter after the controlled isolated-fixture and candidate-contract repairs. The adapter returned **`passed`** for the complete required case set: `field_binding`, `valid_proof`, `replay`, `signature_invalid`, `package_hash_invalid`, `expired`, `authority_mismatch`, and `key_unknown`. This is the retained evidence of case outcomes; no proof, manifest, receipt body, private configuration, secret, key, database URL, or opaque run identifier is reproduced in this record.

The closure repairs remained limited to the source-owned isolated fixture and candidate boundary. They synchronized the fixture’s public device binding from the authorized in-memory signing key; refreshed the collision-free device, signed authority, approved package, assignment, and assignment context idempotently; corrected the assignment-context field-to-asset mapping; and made the isolated temporary package-hash mutation syntactically valid while retaining mandatory restoration. A final repository integrity check now rejects an approved package whose stored hash does not match its canonical definition, allowing the package-hash-invalid case to receive the intended manifest-integrity rejection. The disposable candidate was rebuilt from the reviewed source and its launcher verifies the expected executable SHA-256 before launch.

The final wrapper/adapter run preserved protected acceptance at `/healthz=200` and `/readyz=200` before and after execution, shut down the loopback candidate, and left no listener on `127.0.0.1:18080`. Package enforcement was not enabled. OIDC remained disabled. OpenBao remained sealed and unwired. Repository-wide `go test ./...`, `go vet ./...`, and receipt-propagation structural validation passed after closure, with the source worktree clean. The manifest-delivery hard gate is therefore closed; it does not authorize persistent package-enforcement activation or any production-policy change.
