# INTEGIN Handoff — 2026-08-29

**Base:** `integin-pilot-source` `3d32d7a` (Phase 10) — Ticket 02 `705fb6a` + whole A `2ac15c4→8159b6c` + Phase 8 `d400859` + AI `9dc8299`/`2c7d1c1` + Phase 10 `3d32d7a` — `status 0`

## What was delivered

* **Ticket 02 + whole A:** `workorderpg` 8/8, `workorderhttp` 3/3 `401/200`, `8159b6c` 145 (A1 Keycloak `2ac15c4` 3 files, A2 storage `223ab5c` 2 files, A3 replay `12e5937` 1 file, A4 sync `7f21053` 7 files, A5 docs `72a26ac` 3 files, A6 field-app `6157b5c` 2 files, A7 candidates `8159b6c` 131 files)
* **Phase 8 Quiet Signal:** `quiet-signal` Vite React 19 + TS 6 — advisory-only banner `blocking=false`, evidence-linked signals + reasoning traces, health/canary `baseline 1.26.5`/`candidate 1.27.0`/`12%`, `src/App.tsx:1` `PhotoObservationsPanel` opt-in per photo (Upload directly / Get AI observations — generic for any INTEGIN type, PAUT/EDDY_CURRENT/PECT/TOFD/RT, multi-photo, confidence slider, bbox/heatmap, audit chip, model card), responsive `320px+1fr→mobile` stack — `TSC_OK` `LINT_OK` `vite 201.56kB`, checkpoint `quiet-signal/CHECKPOINT_2026-08-29.md`
* **AI observations:** `ai_service/main.py:15` `AI_FREE_ZONES` deny-list only — `NDT_DEFECT`/`LIFTING_DEFECT` + any future zone/lens via generic default (PAUT/EDDY_CURRENT/PECT/TOFD/RT/MT/PT/ET/MFL/AE), `observations[]` `blocking=false`, `ai_service/test_main.py` 5 passed, design `docs/architecture/INTEGIN_AI_INSPECTION_OBSERVATIONS_DESIGN_2026-08-29.md` (observations only, no hold/keep/edit)
* **Phase 9 Python:** `ai_service` `pytest 5 passed` (`MONITORING`/`REGULATION` + `NDT_DEFECT`/`LIFTING_DEFECT` generic)
* **Phase 10 Handoff:** `go vet` `GOVET_OK`, `go test` `GOTEST_OK` (env stripped), checkpoint `docs/continuity/PHASE_10_HANDOFF_CHECKPOINT_2026-08-29.md`

## How to run

**Console:**
```powershell
cd integin-pilot-source/quiet-signal; npm install; npm run dev
$env:VITE_ADVISORY_API_URL="http://127.0.0.1:8081"; npm run dev
npm run build; npx tsc --noEmit; npx oxlint
```
**Service:**
```powershell
cd integin-pilot-source; pip install -r ai_service/requirements.txt; pip install httpx
python -m pytest ai_service/test_main.py -v
uvicorn ai_service.main:app --host 127.0.0.1 --port 8081
# POST /v1/advisory version:v1 zone:any advisory (NDT_DEFECT etc.) → 200 blocking=false; AI-free 403
```

## Boundaries preserved

`integin_runtime` FORCED RLS, `private\integin-secrets` opaque, package enforcement disabled, OIDC disabled, OpenBao sealed, no `GRANT`/RLS/migration/deployment is claimed by this handoff. Disposable handover closed — `integin-pilot` realm `VERIFY_OK 404`, `integin-keycloak-host-net` retained.

## Next

Owner presents `quiet-signal/CHECKPOINT_2026-08-29.md` + `PHASE_10_HANDOFF_CHECKPOINT_2026-08-29.md` + this handoff; next product track is owner-approved.
